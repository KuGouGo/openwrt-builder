- Worked extensively on the `Rules` repo (<https://github.com/KuGouGo/Rules>) to turn it into a rule conversion/publishing repository based on upstream `nekolsd/sing-geosite` and `nekolsd/geoip`.
- Settled on repo structure centered on top-level `domain/` and `ip/` outputs, with upstream sources under `sources/domain` and `sources/ip`.
- Final rule target formats decided as:
  - Surge: `domain/surge/*.txt` as DOMAIN-SET, `ip/surge/*.txt` as IP ruleset text
  - sing-box: `domain/sing-box/*.srs`, `ip/sing-box/*.srs`
  - mihomo: `domain/mihomo/*.mrs`, `ip/mihomo/*.mrs`
- Important Surge compatibility decision: domain rules should keep upstream DOMAIN-SET semantics directly (one domain per line, leading `.` means domain+subdomains), not be rewritten into `DOMAIN-SUFFIX,...` lines.
- Added custom domain rule pipeline under `sources/domain/custom/` with `emby` and `emby-cn` rules.
- Custom domain rules now automatically generate all three target formats successfully:
  - `domain/surge/emby*.txt`
  - `domain/sing-box/emby*.srs`
  - `domain/mihomo/emby*.mrs`
- Workflow debugging outcome: GitHub Actions `Sync Rules` initially failed on custom sing-box build, then was fixed by adjusting temporary Go module setup in `scripts/build-singbox-custom.sh`; successful run confirmed custom outputs exist.
- README was heavily improved to better reflect reality:
  - attributes upstream sources
  - adds usage examples for Surge / sing-box / mihomo
  - adds structure navigation links and quick links into directories/files
  - corrects mihomo example naming to match actual filenames like `cn.mrs`
